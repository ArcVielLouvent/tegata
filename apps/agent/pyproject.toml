[project]
name = "tegata-agent"
version = "0.1.0"
description = "NLU front-door + hard schema validation for Tegata"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.7,<3",
    "fastapi>=0.111,<1",
    "uvicorn>=0.30,<1",
    "requests>=2.32,<3",
    "python-docx>=1.1,<2",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0,<9",
    "ruff>=0.5,<1",
    "responses>=0.25,<1",
]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.pytest.ini_options]
pythonpath = ["src", "../../packages/schema/python"]
testpaths = ["tests"]
